spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-locations: file://${user.home}/projects/config/{application}/{profile}
server:
  port: 8888

